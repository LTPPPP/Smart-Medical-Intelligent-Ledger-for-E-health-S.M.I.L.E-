@startuml SequenceDiagram
skinparam shadowing false
skinparam sequenceMessageAlign center

title Sequence Diagram — UC-030: Create Work Schedule / On-Call Schedule

actor "Admin" as user
boundary "ScheduleManagementPanel" as form
participant ":DoctorSchedulesController" as ctrl
participant ":DoctorSchedulesService" as schSvc
database "PostgreSQL" as db

user -> form : 1. Fill doctor, clinic, date, shift, room,\nmax patients and submit
activate user
activate form

form -> form : 2. Validate input
activate form
deactivate form
alt validation passes
  form -> ctrl : 3. POST /api/v1/doctor-schedules\n{ doctor_id, clinic_id, work_date, shift_id, room_id, max_patients }
  activate ctrl

  ctrl -> schSvc : 4. create(CreateDoctorScheduleDto)
  activate schSvc

  schSvc -> schSvc : 5. check shift_id provided
  activate schSvc
  deactivate schSvc
  opt shift_id provided
    schSvc -> db : 6. SELECT schedule WHERE doctor_id\nAND work_date AND shift_id
    activate db
    db --> schSvc : DoctorScheduleEntity / null
    deactivate db

    schSvc -> schSvc : 7. check duplicate exists
    activate schSvc
    deactivate schSvc
  end

  alt no duplicate found
    schSvc -> db : 8. INSERT doctor_schedule\n{ doctor_id, clinic_id, work_date, shift_id, room_id, max_patients }
    activate db
    db --> schSvc : DoctorScheduleEntity
    deactivate db

    schSvc --> ctrl : DoctorScheduleEntity
    deactivate schSvc

    ctrl --> form : 9. 201 Created DoctorScheduleEntity
    deactivate ctrl

    form --> user : 10. Display created schedule
    deactivate form
  else duplicate schedule exists
    schSvc --> ctrl : 11. throw ConflictException\n("Doctor already has a schedule for this date and shift")
    deactivate schSvc
    activate ctrl
    ctrl --> form : 12. 409 { message: Duplicate schedule }
    deactivate ctrl
    activate form
    form --> user : 13. Show conflict error
    deactivate form
  end
else validation fails
  form --> user : 14. Show validation errors
  deactivate form
end

deactivate user

@enduml
