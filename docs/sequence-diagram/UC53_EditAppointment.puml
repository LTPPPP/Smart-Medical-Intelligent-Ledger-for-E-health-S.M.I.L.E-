@startuml SequenceDiagram
skinparam shadowing false
skinparam sequenceMessageAlign center

title Sequence Diagram — UC-053: Edit Appointment

actor "Staff" as user
boundary "AppointmentDetailPanel" as form
participant ":AppointmentsController" as ctrl
participant ":AppointmentsService" as aptSvc
database "PostgreSQL" as db

user -> form : 1. Edit appointment fields\n(date, time, doctor, room, notes) and submit
activate user
activate form

form -> ctrl : 2. PATCH /api/v1/appointments/{id}\nUpdateAppointmentDto
activate ctrl

ctrl -> aptSvc : 3. update(id, UpdateAppointmentDto)
activate aptSvc

aptSvc -> db : 4. SELECT appointment WHERE appointment_id\nwith relations
activate db
db --> aptSvc : AppointmentEntity / null
deactivate db

aptSvc -> aptSvc : 5. check appointment exists
activate aptSvc
deactivate aptSvc
alt appointment found
  aptSvc -> db : 6. UPDATE appointment SET changed fields
  activate db
  db --> aptSvc : AppointmentEntity
  deactivate db

  aptSvc --> ctrl : AppointmentEntity
  deactivate aptSvc

  ctrl --> form : 7. 200 OK AppointmentEntity
  deactivate ctrl

  form --> user : 8. Display updated appointment
  deactivate form
else appointment not found
  aptSvc --> ctrl : 9. throw NotFoundException
  deactivate aptSvc
  activate ctrl
  ctrl --> form : 10. 404 { message: Appointment not found }
  deactivate ctrl
  activate form
  form --> user : 11. Show not found error
  deactivate form
end

deactivate user

@enduml
