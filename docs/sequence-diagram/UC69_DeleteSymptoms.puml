@startuml SequenceDiagram
skinparam shadowing false
skinparam sequenceMessageAlign center

title Sequence Diagram — UC-069: Delete Symptoms

actor "Doctor" as user
boundary "SymptomDetailPanel" as form
participant ":SymptomsController" as ctrl
participant ":SymptomsService" as symSvc
database "PostgreSQL" as db

user -> form : 1. Click "Delete Symptom" and confirm
activate user
activate form

form -> ctrl : 2. DELETE /api/v1/symptoms/{symptom_id}
activate ctrl

ctrl -> symSvc : 3. remove(symptom_id)
activate symSvc

symSvc -> db : 4. SELECT symptom WHERE symptom_id
activate db
db --> symSvc : SymptomEntity / null
deactivate db

symSvc -> symSvc : 5. check exists
activate symSvc
deactivate symSvc
alt found
  symSvc -> db : 6. DELETE FROM symptoms WHERE symptom_id
  activate db
  db --> symSvc : void
  deactivate db

  symSvc --> ctrl : void
  deactivate symSvc

  ctrl --> form : 7. 204 No Content
  deactivate ctrl

  form --> user : 8. Display deletion confirmation
  deactivate form
else not found
  symSvc --> ctrl : 9. throw NotFoundException
  deactivate symSvc
  activate ctrl
  ctrl --> form : 10. 404 { message: Symptom not found }
  deactivate ctrl
  activate form
  form --> user : 11. Show not found error
  deactivate form
end

deactivate user

@enduml
