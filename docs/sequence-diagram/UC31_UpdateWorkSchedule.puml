@startuml SequenceDiagram
skinparam shadowing false
skinparam sequenceMessageAlign center

title Sequence Diagram — UC-031: Update Work Schedule / On-Call Schedule

actor "Admin" as user
boundary "ScheduleDetailPanel" as form
participant ":DoctorSchedulesController" as ctrl
participant ":DoctorSchedulesService" as schSvc
database "PostgreSQL" as db

user -> form : 1. Edit schedule fields (shift, room,\nmax patients, status, notes) and submit
activate user
activate form

form -> ctrl : 2. PATCH /api/v1/doctor-schedules/{id}\n{ shift_id?, room_id?, max_patients?, status?, notes?, changed_by, change_reason }
activate ctrl

ctrl -> schSvc : 3. update(id, UpdateDoctorScheduleDto)
activate schSvc

schSvc -> db : 4. SELECT schedule WHERE schedule_id with relations
activate db
db --> schSvc : 5. return DoctorScheduleEntity / null
deactivate db

schSvc -> schSvc : 6. check schedule exists
activate schSvc
deactivate schSvc
alt schedule found
  schSvc -> schSvc : 7. Capture old values\n(shift_id, room_id, max_patients, status, notes)
  activate schSvc
  deactivate schSvc

  schSvc -> db : 8. UPDATE doctor_schedule SET changed fields
  activate db
  db --> schSvc : 9. return updated DoctorScheduleEntity
  deactivate db

  schSvc -> schSvc : 10. Capture new values for audit
  activate schSvc
  deactivate schSvc

  schSvc -> db : 11. INSERT schedule_change\n{ schedule_id, changed_by, change_type: RESCHEDULING,\nold_values, new_values, reason }
  activate db
  db --> schSvc : 12. return ScheduleChangeEntity
  deactivate db

  schSvc --> ctrl : 13. return DoctorScheduleEntity
  deactivate schSvc

  ctrl --> form : 14. 200 OK DoctorScheduleEntity
  deactivate ctrl

  form --> user : 15. Display updated schedule
  deactivate form
else schedule not found
  schSvc --> ctrl : 16. throw NotFoundException
  deactivate schSvc
  activate ctrl
  ctrl --> form : 17. 404 { message: Schedule not found }
  deactivate ctrl
  activate form
  form --> user : 18. Show not found error
  deactivate form
end

deactivate user

@enduml
