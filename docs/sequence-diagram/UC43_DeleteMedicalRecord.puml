@startuml SequenceDiagram
skinparam shadowing false
skinparam sequenceMessageAlign center

title Sequence Diagram — UC-043: Delete Medical Record

actor "Doctor" as user
boundary "MedicalRecordDetailPanel" as form
participant ":MedicalRecordsController" as ctrl
participant ":MedicalRecordsService" as recSvc
database "PostgreSQL" as db

user -> form : 1. Click "Delete Record" and confirm
activate user
activate form

form -> ctrl : 2. DELETE /api/v1/medical-records/{record_id}
activate ctrl

ctrl -> recSvc : 3. remove(record_id)
activate recSvc

recSvc -> db : 4. SELECT medical_record WHERE record_id
activate db
db --> recSvc : 5. return MedicalRecordEntity / null
deactivate db

recSvc -> recSvc : 6. check record exists
activate recSvc
deactivate recSvc
alt record found
  recSvc -> db : 7. DELETE FROM medical_records WHERE record_id
  activate db
  db --> recSvc : 8. return deleted (affectedRows: 1)
  deactivate db

  recSvc --> ctrl : 9. return success
  deactivate recSvc

  ctrl --> form : 10. 204 No Content
  deactivate ctrl

  form --> user : 11. Display deletion confirmation
  deactivate form
else record not found
  recSvc --> ctrl : 12. throw NotFoundException
  deactivate recSvc
  activate ctrl
  ctrl --> form : 13. 404 { message: Medical record not found }
  deactivate ctrl
  activate form
  form --> user : 14. Show not found error
  deactivate form
end

deactivate user

@enduml
