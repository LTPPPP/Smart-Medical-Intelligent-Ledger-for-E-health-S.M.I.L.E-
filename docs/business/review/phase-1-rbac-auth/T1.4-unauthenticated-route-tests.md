# T1.4 — Behavioral tests: unauthenticated + wrong-role calls

**Status:** ⬜ BLOCKED (needs running stack) · **Size:** L · **Priority:** 🔴 P0
**Source:** BUSINESS_FLOW_SOURCE.md §10 Final P0 row 1, §12 Final validation matrix.

> **Predicted results from static analysis (T1.2/T1.3/T1.6) — confirm at runtime:**
> - A1–A7 (unauth clinical) → **401 expected PASS** (downstream JwtAuthGuard).
> - A-notifications (`GET /api/v1/notifications`, not yet a row — add it) → **200, FAIL (F1-001)**.
> - B3/B4 (Reception POST diagnosis/prescription) → **403 PASS**; B5/B6 (Nurse final) → **403 PASS**.
> - O1/O3/O4 (Doctor A reads Doctor B / other-patient data) → **200, FAIL (F1-002)**.
> - C1/C2 (header spoof) → **401/role-preserved PASS** (gateway strips headers).
> Add row **A11: `GET /api/v1/notifications` → expect 401, predict 200 (F1-001)**.

Runtime confirmation of T1.2/T1.3 static findings. Run against a locally started stack
(`docker-compose.yml` / `Makefile` — document exact startup used).

## Prerequisites

- [ ] 0a. Stack running locally (gateway + iam + clinical-emr + payment + db). Record how.
- [ ] 0b. Seeded test accounts, one per role (seeds exist:
      `iam-service/src/database/seeds/relational/run-seed.ts` — has NURSE etc.). Record credentials/ids.
- [ ] 0c. Helper script to login per role and capture JWT → `scripts/` or alongside this file.

## Test matrix A — Unauthenticated (expect **401** on every row)

Via the **gateway** (not direct service port — that's matrix C):

| # | Request | Expected | Actual | Verdict |
|---|---------|----------|--------|---------|
| A1 | `GET /api/v1/patients` | 401 | | |
| A2 | `GET /api/v1/medical-records` | 401 | | |
| A3 | `GET /api/v1/examination-sessions` | 401 | | |
| A4 | `GET /api/v1/dental-images` | 401 | | |
| A5 | `GET /api/v1/prescriptions` | 401 | | |
| A6 | `GET /api/v1/treatment-plans` | 401 | | |
| A7 | `GET /api/v1/appointments` | 401 | | |
| A8 | `GET /api/v1/payments` (list/history) | 401 | | |
| A9 | `GET /api/v1/audit-logs` | 401 | | |
| A10 | File download URL of an uploaded dental image (raw) | 401/403 | | |

## Test matrix B — Wrong role (expect **403**)

| # | Actor | Request | Expected | Actual | Verdict |
|---|-------|---------|----------|--------|---------|
| B1 | PATIENT | `GET /api/v1/examination-sessions` (all) | 403 or own-only | | |
| B2 | PATIENT | `GET /api/v1/patients` (list all) | 403 | | |
| B3 | RECEPTIONIST | `POST /api/v1/diagnoses` | 403 | | |
| B4 | RECEPTIONIST | `POST /api/v1/prescriptions` | 403 | | |
| B5 | NURSE | finalize/sign prescription or session | 403 | | |
| B6 | NURSE | `POST /api/v1/diagnoses` (final) | 403 | | |
| B7 | DOCTOR | `GET /api/v1/audit-logs` | 403 | | |
| B8 | DOCTOR | approve refund / user management | 403 | | |
| B9 | PATIENT | `GET /api/v1/appointments?patient_id=<someone else>` | 403/404/own-only | | |
| B10 | RECEPTIONIST | `GET` clinical detail (diagnosis/exam note content) | 403 per I8 ("admin info only") | | |

## Test matrix C — Header spoofing (expect **401/403**, depends on T1.2 #4)

| # | Request | Expected | Actual | Verdict |
|---|---------|----------|--------|---------|
| C1 | No token + `x-auth-role: ADMIN` header via gateway | 401 | | |
| C2 | PATIENT token + `x-auth-role: ADMIN` header override | role stays PATIENT | | |
| C3 | Direct call to clinical-emr service port bypassing gateway (if reachable in deploy topology) | 401 or network-unreachable | | |

## Deliverables

- [ ] `curl`-based script or HTTP file checked in next to this task (so it can be re-run after fixes).
- [ ] Every ❌ row copied to FINDINGS.md with response body snippet.
- [ ] Note which rows should graduate into automated e2e tests in Phase 5.

## Evidence

_(commands + responses)_

## Verdict

_(summary)_
