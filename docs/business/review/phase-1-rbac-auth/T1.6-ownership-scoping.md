# T1.6 — Ownership / data scoping

**Status:** 🔄 CODE DONE, runtime pending · **Size:** L · **Priority:** 🔴 P0
**Source:** BUSINESS_FLOW_SOURCE.md §6.2–6.5, §11.6, §10 Final P0 row 2, T-DOC-001/003/010.
**Verdict:** ✅ appointments scoped well · ❌ F1-002 (exam-sessions/records/etc. by-id NOT scoped) · runtime probes O1–O5 → T1.4

## Results (08/07/2026) — backend

1. **examination-sessions ❌** — `GET /doctor/:doctor_id` (`:47-49`) and `/patient/:patient_id`
   (`:42`) pass the URL id straight to the service; no `doctor_id == actor` check. Doctor A can read
   Doctor B's sessions. → **F1-002**. (Role gate ADMIN/DOCTOR stops patients, not cross-doctor.)
2. **appointments ✅** — `appointments.service.ts.findAll` (`:464-508`) scopes patient→own,
   doctor→own (403 if `query.doctor_id != actor`), requires staff role otherwise. This is the
   **reference pattern** the other modules should copy.
3. **medical-records ❌** — `GET /patient/:patient_id` (`:39-41`) no relation-to-patient / own-record
   check. → F1-002.
4. **prescriptions / treatment-plans / diagnoses / dental-images ❌** — same by-patient/by-doctor
   shape, role-gated to ADMIN/DOCTOR, no actor-ownership check. → F1-002.
5. **reports** — `DASHBOARD_DOCTOR` param scoping not yet traced; flag for Phase 3 (T-DOC dashboard).
6. **doctor-schedules/doctor-leaves** — role-gated ADMIN/DOCTOR/RECEPTIONIST; self-vs-approve
   separation not verified at code level → check in Phase 3 (leave approve must be ADMIN/manager).
7. **payments** — patient-vs-own and refund initiate/approve split → verify in T2.7.

## Frontend symptom confirmation
8–10. Doctor-selector / hardcoded `CURRENT_USER_ID` — not re-checked this pass (backend is the
authority for F1-002). Defer to Phase 3 Doctor UI review.

## Runtime probes O1–O5 → run in T1.4 (need stack). Expected: O1/O3/O4 currently **fail** (200 instead of 403) per F1-002.


## Doc claim (30/06/2026)

Role checks aside, **nothing scoped data to the current user**:
- Appointments page loaded all appointments (`useAppointmentsList({limit: 50})`), no `doctor_id=currentUser`.
- Examinations page loaded ALL `/examination-sessions`.
- DoctorDashboard + My Schedule let the doctor **pick any doctor** from a seed `DOCTORS` list;
  backend trusted client-supplied `doctor_id`.
- Leaves page hardcoded `approvedBy: 'CURRENT_USER_ID'`.

## Checklist — backend (the part that matters)

- [x] 1. examination-sessions ❌ — `/doctor/:doctor_id` & `/patient/:patient_id` pass URL id, no actor check → cross-doctor read. **F1-002**.
- [x] 2. appointments ✅ — `findAll` (`service.ts:464-508`) scopes patient→own, doctor→own (403 on mismatch). Reference pattern.
- [x] 3. medical-records ❌ — `/patient/:patient_id` (`:39`) no relation/own check. **F1-002**. (Patient has no read path at all — role-gated ADMIN/DOCTOR.)
- [x] 4. prescriptions/treatment-plans/diagnoses/dental-images ❌ — same unscoped by-id shape. **F1-002**.
- [~] 5. reports `DASHBOARD_DOCTOR` scoping not fully traced → Phase 3 (T-DOC dashboard).
- [~] 6. doctor-schedules/leaves role-gated ADMIN/DOCTOR/RECEPTIONIST; self-vs-approve split not verified → Phase 3.
- [~] 7. payments patient-own + refund initiate/approve split → verify T2.7.

## Checklist — frontend (symptom confirmation)

- [~] 8. Doctor-selector re-check deferred to Phase 3 Doctor UI (backend is authority for F1-002).
- [~] 9. Appointments/examinations page filters → Phase 3.
- [~] 10. `approvedBy: 'CURRENT_USER_ID'` hardcode → Phase 3 (leave flow).

## Runtime probes (fold into T1.4 run)

| # | Actor | Request | Expected |
|---|-------|---------|----------|
| O1 | Doctor A | `GET /examination-sessions/doctor/<doctorB_id>` | 403 |
| O2 | Doctor A | `GET /appointments?doctor_id=<doctorB_id>` | 403 or silently scoped to A |
| O3 | Patient X | `GET /medical-records/<patientY_record>` | 403/404 |
| O4 | Doctor A | `GET /dental-images/patient/<patient not assigned>` | 403 |
| O5 | Doctor A | `PATCH /doctor-leaves/<own>/approve` (self-approve) | 403 |

## Evidence

_(file:line + probe outputs)_

## Verdict

_(✅ / ❌ / 🟢 per item; ❌ → FINDINGS.md)_
