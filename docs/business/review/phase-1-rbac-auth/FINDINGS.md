# Phase 1 — Findings Log

Confirmed findings, re-verified on branch `feat/chinh/testing-business` (08/07/2026).
Severity: 🔴 Critical (unauth access to PHI/PII or privilege escalation) · 🟠 High (role exceeds
business permission / cross-tenant data leak) · 🟡 Medium (scoping/UX/consistency).

Static code review complete (T1.1–T1.3, T1.5, T1.6-code, T1.7). Runtime confirmation (T1.4 + the
runtime probes in T1.5/T1.6) still pending a locally running stack.

## Confirmed findings

| ID | Sev | Finding | Evidence (file:line) | Business/legal ref | Status |
|----|-----|---------|----------------------|--------------------|--------|
| F1-001 | 🔴 | ✅ **FIXED** (guards added: create=internal-key, reads=JWT, templates=ADMIN; gateway requires identity; publishers send `x-internal-api-key`). **Notifications endpoints have no auth guard at all.** `notifications`, `notification-templates`, `notification-preferences` controllers declare no `@UseGuards`. They are routed through the gateway (`services.config.ts:52-54`), and the gateway does **not** require a token for these prefixes (`requiresTrustedIdentity` only covers booking + appointments + patient-representatives). Result: unauthenticated read/write/delete of notifications (appointment reminders contain patient name + time + place = PII). | `iam-service/.../notifications/notifications.controller.ts:28-…` (no guards); `gateway/.../proxy.middleware.ts:79-85`; `gateway/.../services.config.ts:52-54` | PL-05 (Luật BVDLCN 2025) Đ3; Final §5.2 reminder rules | OPEN |
| F1-002 | 🟠 | 🔄 **PARTIAL FIX** (scoped: examination-sessions, medical-records, prescriptions; remaining: treatment-plans/diagnoses need session-join, dental-images needs ownership decision). **No ownership scoping on clinical read-by-id routes.** Controllers take `doctor_id`/`patient_id` straight from the URL and query by it, with only `@Roles(ADMIN, DOCTOR)` — no check that the id belongs to the calling actor. So Doctor A can read Doctor B's sessions/records/prescriptions/images. | `examination-sessions.controller.ts:42,47` (`patient/:patient_id`, `doctor/:doctor_id` — no actor check); `medical-records.controller.ts:39`; also diagnoses/prescriptions/treatment-plans/dental-images by-patient/by-doctor | §11.6 Ownership rule; §10 Final P0 row 2; T-DOC-001/003/010 | OPEN |
| F1-003 | 🟠 | **Permission-grant model exists in DB but is NOT used for authorization.** iam-service has `role.entity` / `permission.entity` / `role-permission.entity` (the role→permission "tag/grant" join) + seeds (`run-user-seed.ts:178`). But the JWT only carries a single `role` string (`auth.service.ts:456-465` `getTokensData` takes only `role`); no permissions are put in the token. Every backend guard authorizes by hardcoded `@Roles(RoleEnum.X)`, **ignoring the grant table**. Frontend `ProtectedRoute` checks `user.permissions`, but those are never populated from grants. → Admin's "Role Management / grant permission to role" screen has no runtime effect on access control. | `iam-service/roles`,`/permissions`,`/user-roles` entities; `auth.service.ts:456-465`; `jwt.strategy.ts:49`; clinical `roles.guard.ts` (role-only); FE `ProtectedRoute.tsx:75-83` | K2 RBAC matrix; §K1-K2 | OPEN |
| F1-004 | 🟠 | **DOCTOR (backend) vs DENTIST (frontend) is a functional break, not just naming.** Backend JWT role is `"DOCTOR"` (RoleEnum has no DENTIST). Frontend gates clinical features on `"DENTIST"`: `CLINICAL_ROLES = ["DENTIST","CLINIC_ADMIN"]` and `config/navigation.ts:21,28,35,42` list `"DENTIST"`. A real doctor logs in with role `"DOCTOR"` → fails these checks → clinical nav/features hidden. (`resolveDashboardKind` in nav.ts handles both, so dashboards work, but the role-list gates don't.) | BE `accounts/domain/account.ts:12-18`; FE `shared/constants/roles.ts:9,31`; `config/navigation.ts:21-42` | ROLE_BENCHMARK §0 note | OPEN |
| F1-005 | 🟡 | **Check-in allowed for DOCTOR and NURSE, not just Receptionist.** `appointments.service.ts` check-in path permits any `isPrivilegedStaffRole` (ADMIN/RECEPTIONIST/NURSE) **or** DOCTOR. Business (§I2, RACI §11.2, Final §4) makes check-in a Receptionist/ops action. Minor, but it's role-scope drift. Note appointments controller has **no `@Roles`** at all — it relies entirely on service-layer actor checks (which are otherwise good — see note below). | `appointments.controller.ts:296-320` (no @Roles, header-based); `appointments.service.ts:286-320` | §I2; §11.2 RACI | OPEN |
| F1-006 | 🟡 | **Frontend route protection is presence-only + inconsistently applied.** `middleware.ts` only checks the `access_token` cookie **exists** (no signature/expiry/role validation) and redirects. Only 3 pages use `ProtectedRoute`/`ProtectedLayout` with roles/permissions (`admin/layout.tsx`, `profile`, medical-records pages); most `(pages)/*` have no route-level role guard and rely on the API. `DOCTOR_ROUTES`/`RECEPTIONIST_ROUTES`/`NURSE_ROUTES` allowlists in `routes.ts:132-140` are **dead code** (not referenced anywhere). | `middleware.ts:16-35`; `routes.ts:132-140` (unused); grep: ProtectedRoute in 3 files only | §6.1; defense-in-depth | OPEN |
| F1-007 | 🟡 | **NURSE has effectively no clinical grants.** No controller `@Roles` includes NURSE; NURSE only appears in service allowlists (appointment create/list/check-in, payment, patient-reps). Cannot record vitals into an examination session (business J2 expects nurse to). Confirms doc/benchmark claim; feeds Phase 3 Nurse review. | grep `NURSE` across `*.controller.ts` = none; `run-user-seed.ts:178` grants only `patient:read, appointment:read` | §J2, §J8; ROLE_BENCHMARK §4.3 | OPEN |
| F1-008 | 🟡 | ✅ patient-reps **confirmed intentional** (comment at `controller.ts:18-19`: PATIENT must reach these to manage own reps; adding `@Roles` would break that; ownership enforced in service). ⏸️ **`files` local download (`GET :path`) has NO guard** — serves any file from `./files` by path, unauthenticated. Real hole, but naive `JwtAuthGuard` breaks browser `<img src>` rendering (FE renders images via direct `<img src={image_url}>`). Proper fix = signed URLs / token-in-query; deferred to the dental-image/file-storage workstream. | `files/infrastructure/uploader/local/files.controller.ts` (download unguarded); FE `dental-image/components/ImageAnnotation.tsx:94-95` | PL-01 Đ69.2; §5.7 | ⏸️ deferred (files) |

## Positive / already-fixed since the doc's audit (🟢 — for the compliance report)

| Doc claim (30/06) | Current state (08/07) | Evidence |
|-------------------|------------------------|----------|
| `DISABLE_AUTH_GUARD = true`, frontend middleware bypasses auth | Guard active (presence-based redirect to /login) | `middleware.ts:14-35` |
| No RolesGuard/APP_GUARD in clinical-emr; `@Roles` is inert metadata | `RolesGuard` implemented and applied via `@UseGuards(JwtAuthGuard, RolesGuard)` on ~29 controllers; guard reads `request.actor.role` | `auth/roles/roles.guard.ts`; controller sweep |
| Gateway only requires token for booking/appointments → other medical routes open | **Not exploitable:** each downstream service re-verifies the HS256 JWT itself via its own `JwtAuthGuard` (`actor.util.ts` — explicitly does not trust gateway headers), and the gateway **strips** inbound `x-auth-*` headers before injecting verified ones. Header spoofing blocked. Exception = F1-001 (notifications have no downstream guard either). | `proxy.middleware.ts:171-184`; `clinical-emr/auth/actor.util.ts:9-16`; `jwt-auth.guard.ts:20-32` |
| Examination session missing `appointment_id`; no finalize/amendments routes | Fields + routes exist (`appointment_id` on entity/DTO; `finalize`, `amendments` endpoints) — behavior verified in Phase 2 | `examination-session.entity.ts:20`; controller `:52,64,69,77` |
| Appointments load-all, no doctor/patient scoping | `appointments.service.ts.findAll` scopes patient→own, doctor→own, requires staff role otherwise (good pattern the other modules should copy) | `appointments.service.ts:464-508` |
| diagnoses/prescriptions had no `@Roles` | Both now `@Roles(ADMIN, DOCTOR)` | controller sweep |

## Intent mismatches to resolve with the team (feed T1.5 matrix, not necessarily bugs)

- `examination-sessions` = `@Roles(ADMIN, DOCTOR)`. K2 says Admin should NOT have `examination.write`; and Nurse should draft vitals (J2). Neither is reflected. Decide intended matrix.
- `services` / `doctor-specialties` allow `RECEPTIONIST` write — confirm intended (catalog editing is usually Admin per K5).
- `kyc-verifications` allows `RECEPTIONIST` (`@Roles(ADMIN, RECEPTIONIST)`) — but business pauses KYC out of core and gives KYC approval to Admin only (§K3). Confirm scope.
