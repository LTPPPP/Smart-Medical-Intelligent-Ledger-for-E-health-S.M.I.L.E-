// ============================================================
// Test Page — database & API connectivity testing
// ============================================================

import { TestContent } from "./TestContent";

export const metadata = {
    title: "Connection Test | S.M.I.L.E",
    description: "Test connectivity between Frontend and Backend services",
};

export default function TestPage() {
    return <TestContent />;
}
